
// src/assets.ts

// BRAND_MASCOT_SRC will point to "5.png" at the root of the "dist" directory after build.
// The build script copies contents of "public/" (where 5.png should be) to "dist/".
export const BRAND_MASCOT_SRC = "5.png";

// MI_IMAGEN_SRC will now also use 5.png.
// This will also point to "5.png" at the root of the "dist" directory.
export const MI_IMAGEN_SRC = "5.png";
